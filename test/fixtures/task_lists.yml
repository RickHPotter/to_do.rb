# == Schema Information
#
# Table name: task_lists
#
#  id             :bigint           not null, primary key
#  task_list_name :string           not null
#  description    :text
#  policy         :integer          default(0), not null
#  progress       :integer          default(0), not null
#  priority       :integer          default(0), not null
#  due_date       :date             not null
#  team_id        :bigint           not null
#  creator_id     :bigint           not null
#  created_at     :datetime         not null
#  updated_at     :datetime         not null
#

today:
  creator: lovelace
  task_list_name: Today
  description: We need to get these things done ASAP before I yet again create a new amazing masterpiece that is another task_list
  team: sept
  policy: 1
  progress: 1
  priority: 1
  due_date: <%= Date.current %>

never:
  creator: a
  task_list_name: Never
  description: Everything that never should happen
  team: mizu
  policy: 0
  progress: 1
  priority: 1
  due_date: <%= Date.tomorrow %>
