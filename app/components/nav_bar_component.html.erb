<nav class="pt-2">
  <div class="rounded-t-lg bg-purple-360 px-2 sm:px-6 lg:px-8">
    <div class="relative flex h-16 items-center justify-between">
      <%= render @logo_partial %>
      <div class="flex flex-1 items-start sm:items-stretch sm:justify-start">
        <div class="hidden sm:ml-6 sm:block">
          <div class="flex space-x-4">
            <% { 'Teams' => teams_path, 'Projects' => projects_path }.each_pair do |name, link| %>
              <%= link_to name, link, class: "#{request.path == link ? 'text-orange-360' : 'text-white hover:text-orange-360'} rounded-md px-3 py-2 text-sm font-medium" %>
            <% end %>
          </div>
        </div>
      </div>

      <div class="absolute inset-y-0 right-0 flex items-center pr-2 sm:static sm:inset-auto sm:ml-6 sm:pr-0">
        <%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete }, class: 'text-white hover:text-orange-360 rounded-md px-3 py-2 text-sm font-medium' %>

        <div class="relative ml-3">
          <div>
            <button type="button" class="relative flex rounded-full bg-gray-800 text-sm focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800" id="user-menu-button" aria-expanded="false" aria-haspopup="true">
              <span class="absolute -inset-1.5"></span>
              <img class="h-8 w-8 rounded-full" src="https://images.unsplash.com/photo-1610296669228-602fa827fc1f?q=80&w=1675&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D" alt="">
            </button>
          </div>
        </div>

      </div>
    </div>
  </div>
</nav>
